/* ==========================================================================
   Assur Chap — Configuration front-end
   Copiez ce fichier en `config.js` et renseignez vos clés Supabase.
   En mode démo (valeurs vides), l'app fonctionne sur des données locales.
   ========================================================================== */
window.AC_CONFIG = {
  // Projet Supabase (Dashboard → Settings → API)
  SUPABASE_URL: "",        // ex: https://xxxxxxxx.supabase.co
  SUPABASE_ANON_KEY: "",   // clé publique "anon"

  // URL des Edge Functions (par défaut: SUPABASE_URL + /functions/v1)
  FUNCTIONS_URL: "",

  // URL publique de l'app (pour les liens de vérification / retour paiement)
  APP_URL: ""
};
